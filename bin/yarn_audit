#!/bin/sh

NODE_NO_WARNINGS=1 yarn audit
EXIT_CODE=$?

if [ $EXIT_CODE -ge 8 ]; then
  echo "High or Critical vulnerabilities found"
  NODE_NO_WARNINGS=1 yarn audit --level high
  exit 1
fi

echo "No high or critical vulnerabilities found"
